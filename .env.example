# Resend (https://resend.com) — used by /api/inquiry to send the internal
# notification + submitter confirmation for the Start a Project form.
# Requires a verified sending domain for the FROM_ADDRESS in that route.
RESEND_API_KEY=
